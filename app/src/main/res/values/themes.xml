<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FotosPremium" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item> <!-- Color texto CarView y btn descargar -->
        <item name="colorPrimaryVariant">@color/black</item>  <!-- Color Barra Hora Superior -->
        <item name="colorOnPrimary">@color/white</item>  <!-- Color Barra Toolbar  -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_barra_superpuesta</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FotosPremium.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FotosPremium.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FotosPremium.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>